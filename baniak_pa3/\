#include <stdio.h>

int main() {
	int n = 100001;
	int sieve[n];
	int p;

	for(p=0; p < 10; p++){
		sieve[p] = p+2;
		printf("%d \n", sieve[p]);
	}
	
	for(p=0; p < 10; p++){
		sieve[p*2] = 0;
	}

	 for(p=0; p < 10; p++){
	 	printf("%d \n", sieve[p]);
	}

	return 0;
}
