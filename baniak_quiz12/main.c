#include <stdio.h>

int main(){
	int n = 0;
	int aa = 0;
	
	printf("Size of array:");
	scanf("%d", n);

	a = (int*) malloc( n * sizeof(int));

	printf("\nEnter that many integers: ");
	
	for(

	return 0;
}
